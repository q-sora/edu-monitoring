# requirements.txt — pinned for reproducibility
# Install: pip install -r requirements.txt

# ── Core framework ────────────────────────────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.1    # includes websockets + httptools
gunicorn==22.0.0             # production process manager

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.30  # 2.0 style (Mapped[], mapped_column())
asyncpg==0.29.0              # async PostgreSQL driver (binary protocol)
alembic==1.13.1              # migrations

# ── Validation ────────────────────────────────────────────────────────────────
pydantic==2.7.4
pydantic-settings==2.3.4

# ── Auth ──────────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT decode/verify (HS256)
passlib[bcrypt]==1.7.4             # password hashing (if managing own users)

# ── Redis / Caching ───────────────────────────────────────────────────────────
redis[hiredis]==5.0.6          # async client + C-extension for speed
celery[redis]==5.4.0           # background task queue

# ── HTTP client (for external integrations: НОБД, ЕПВО, Gemini) ──────────────
httpx==0.27.0

# ── Data processing (Excel import / catalog parsing) ──────────────────────────
pandas==2.2.2
openpyxl==3.1.3           # pandas Excel backend (.xlsx)
rapidfuzz>=3.0.0          # fuzzy column matching for universal import

# ── Observability ─────────────────────────────────────────────────────────────
structlog==24.2.0              # structured JSON logging
opentelemetry-sdk==1.25.0      # distributed tracing (Jaeger / Tempo)
opentelemetry-instrumentation-fastapi==0.46b0
opentelemetry-instrumentation-sqlalchemy==0.46b0
prometheus-fastapi-instrumentator==7.0.0

# ── Development only ──────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0           # test client for async FastAPI
factory-boy==3.3.0      # fixture factories
